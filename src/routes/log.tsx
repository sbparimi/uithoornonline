import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { AppShell } from "@/components/AppShell";
import { Plane, ChevronRight, ShieldCheck, Clock3, MapPin, FileText } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
import { useAuth } from "@/hooks/use-auth";
import { toast } from "sonner";
import { featureFlags } from "@/config/featureFlags";

export const Route = createFileRoute("/log")({ component: LogPage, head: () => ({ meta: [
  { title: "Geluidshinder melden — uithoorn.online" },
  { name: "description", content: "Registreer een bewonersmelding met tijdstempel, optioneel vluchtnummer en locatie." },
] }) });

type Log = { id: string; timestamp: string; observed_at: string | null; flight_number: string | null; altitude: number | null; db_level: number | null; duration_seconds: number | null; observation_text: string | null; lat: number | null; lng: number | null; location_source: "browser" | "resident" | "none" | null };

function LogPage() {
  const { user, loading } = useAuth(); const navigate = useNavigate();
  const [now, setNow] = useState(new Date()); const [logs, setLogs] = useState<Log[]>([]); const [last, setLast] = useState<Log | null>(null);
  const [submitting, setSubmitting] = useState(false); const [db, setDb] = useState(70); const [flight, setFlight] = useState(""); const [description, setDescription] = useState(""); const [duration, setDuration] = useState(""); const [geoErr, setGeoErr] = useState<string | null>(null);
  useEffect(() => { const t = setInterval(() => setNow(new Date()), 1000); return () => clearInterval(t); }, []);
  useEffect(() => { if (!user) return; supabase.from("noise_logs").select("*").order("timestamp", { ascending: false }).limit(20).then(({ data }) => setLogs((data as Log[]) ?? [])); }, [user]);
  const getCoords = () => new Promise<{lat:number;lng:number}|null>(resolve => { if (typeof navigator === "undefined" || !navigator.geolocation) return resolve(null); navigator.geolocation.getCurrentPosition(p => resolve({lat:p.coords.latitude,lng:p.coords.longitude}), () => resolve(null), {timeout:4000,maximumAge:60000}); });
  const submit = async () => {
    if (!user) { navigate({ to: "/auth", search: { next: "/log" } as any }); return; }
    setSubmitting(true); setGeoErr(null); const coords = await getCoords();
    if (!coords) setGeoErr("Locatie niet beschikbaar. De bewonersmelding wordt zonder coördinaten opgeslagen.");
    const durationSeconds = duration.trim() ? Number.parseInt(duration, 10) * 60 : null;
    const observedAt = new Date().toISOString();
    const payload = featureFlags.noiseEvidenceV2 ? { user_id:user.id, flight_number:flight.trim()||null, altitude:null, db_level:db, lat:coords?.lat??null, lng:coords?.lng??null, observed_at:observedAt, duration_seconds:Number.isFinite(durationSeconds)?durationSeconds:null, observation_text:description.trim()||null, location_source:coords?"browser":"none" } : { user_id:user.id, flight_number:flight.trim()||null, altitude:null, db_level:db, lat:coords?.lat??null, lng:coords?.lng??null };
    const { data, error } = await supabase.from("noise_logs").insert(payload).select().single();
    setSubmitting(false); if (error) { toast.error("Kon melding niet opslaan"); return; }
    const row=data as Log; setLast(row); setLogs(p=>[row,...p]); setFlight(""); setDescription(""); setDuration(""); toast.success("Bewonersmelding geregistreerd"); if(navigator.vibrate) navigator.vibrate(40);
  };
  return <AppShell>
    <section className="bg-navy px-5 pb-8 pt-7 text-navy-foreground sm:px-6 lg:px-8"><div className="mx-auto max-w-3xl"><div className="flex items-center gap-2 text-[10px] font-semibold uppercase tracking-[0.16em] text-white/55"><span className="h-1.5 w-1.5 rounded-full bg-red" /> Bewonersmelding</div><h1 className="mt-3 text-3xl sm:text-4xl">Geluid melden</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-white/65">Leg vast wat je nu ervaart. Je melding krijgt een echte tijdstempel en kan onderdeel worden van je eigen dossier.</p><div className="mt-5 flex items-center gap-2 text-xs text-white/55"><Clock3 size={14} /> {now.toLocaleString("nl-NL")}</div></div></section>
    <section className="mx-auto -mt-5 max-w-3xl px-5 pb-5 sm:px-6"><div className="rounded-2xl border border-border bg-white p-5 shadow-[0_18px_50px_rgba(13,31,60,.10)] sm:p-6"><div className="mb-5 flex items-start gap-3 rounded-xl border border-navy/10 bg-cream px-4 py-3"><ShieldCheck size={18} className="mt-0.5 shrink-0 text-navy/65" /><div><div className="text-xs font-semibold text-navy">Bewonersmelding, geen officiële meting</div><div className="mt-0.5 text-[11px] leading-5 text-navy/55">Je legt je eigen waarneming vast. De waarde hieronder is een schatting en geen gekalibreerde geluidsmeting.</div></div></div><div><div className="flex items-baseline justify-between"><label className="text-xs font-semibold text-navy">Geschat geluidsniveau</label><span className="font-mono text-lg font-semibold text-navy">{db} <span className="text-xs font-normal">dB</span></span></div><input aria-label="Geschat geluidsniveau in decibel" type="range" min={50} max={95} value={db} onChange={e=>setDb(parseInt(e.target.value,10))} className="mt-4 w-full accent-red" /><div className="flex justify-between text-[10px] text-muted-foreground"><span>50</span><span>75</span><span>95</span></div></div>{featureFlags.noiseEvidenceV2&&<><div className="mt-5"><label className="text-xs font-semibold text-navy" htmlFor="noise-description">Wat heb je ervaren? <span className="font-normal text-muted-foreground">(optioneel)</span></label><textarea id="noise-description" value={description} onChange={e=>setDescription(e.target.value)} placeholder="Bijv. zeer luid vliegtuiggeluid, televisie moeilijk verstaanbaar…" maxLength={1000} rows={4} className="mt-1.5 w-full resize-none rounded-xl border border-border bg-cream px-3 py-3 text-sm outline-none"/><div className="mt-1 text-right text-[10px] text-muted-foreground">{description.length}/1000</div></div><div className="mt-4"><label className="text-xs font-semibold text-navy" htmlFor="noise-duration">Duur van de ervaring <span className="font-normal text-muted-foreground">(optioneel, minuten)</span></label><input id="noise-duration" inputMode="numeric" type="number" min="1" max="1440" value={duration} onChange={e=>setDuration(e.target.value)} placeholder="Bijv. 3" className="mt-1.5 w-full rounded-xl border border-border bg-cream px-3 py-3 text-sm outline-none"/></div></>}<div className="mt-5"><label className="text-xs font-semibold text-navy">Vluchtnummer <span className="font-normal text-muted-foreground">(optioneel)</span></label><input value={flight} onChange={e=>setFlight(e.target.value.toUpperCase())} placeholder="Bijv. KL1234" className="mt-1.5 w-full rounded-xl border border-border bg-cream px-3 py-3 text-sm outline-none" maxLength={12}/><p className="mt-1.5 text-[11px] leading-5 text-muted-foreground">Een opgegeven vluchtnummer is jouw melding. De app stelt daarmee niet vast dat deze vlucht de hinder heeft veroorzaakt.</p></div><button onClick={submit} disabled={submitting||loading} className="mt-5 w-full rounded-xl bg-red px-4 py-4 font-semibold text-red-foreground shadow-[0_8px_24px_-10px_rgba(255,60,42,.7)] disabled:opacity-60"><span className="flex items-center justify-center gap-2"><Plane size={18}/>{submitting?"Bezig…":"Registreer bewonersmelding"}</span><span className="mt-1 block text-[10px] text-red-foreground/75">{now.toLocaleTimeString("nl-NL")}</span></button>{geoErr&&<div className="mt-3 flex gap-2 rounded-xl bg-amber-50 p-3 text-[11px] leading-5 text-amber-900"><MapPin size={14} className="mt-0.5 shrink-0"/>{geoErr}</div>}</div></section>
    {last&&<section className="mx-auto max-w-3xl px-5 pb-5 sm:px-6"><div className="rounded-2xl border border-border bg-white p-5"><div className="text-[10px] font-semibold uppercase tracking-[0.14em] text-red">Laatst geregistreerd</div><div className="mt-1 flex items-baseline justify-between"><h2 className="text-xl text-navy">{last.flight_number??"Vlucht onbekend"}</h2><span className="text-xs text-muted-foreground">{new Date(last.observed_at??last.timestamp).toLocaleTimeString("nl-NL")}</span></div><div className="mt-4 grid grid-cols-2 gap-2"><div className="rounded-xl bg-cream p-3"><div className="text-[9px] uppercase tracking-wider text-muted-foreground">Tijdstip</div><div className="mt-1 font-mono text-xs text-navy">{new Date(last.observed_at??last.timestamp).toLocaleString("nl-NL")}</div></div><div className="rounded-xl bg-cream p-3"><div className="text-[9px] uppercase tracking-wider text-muted-foreground">Geschat geluid</div><div className="mt-1 font-mono text-sm text-navy">{last.db_level} dB</div></div></div>{featureFlags.noiseEvidenceV2&&(last.observation_text||last.duration_seconds)&&<div className="mt-2 rounded-xl bg-cream p-3"><div className="flex items-center gap-2 text-[9px] uppercase tracking-wider text-muted-foreground"><FileText size={12}/> Eigen waarneming</div>{last.observation_text&&<p className="mt-1 text-sm leading-5 text-navy">{last.observation_text}</p>}{last.duration_seconds&&<p className="mt-1 text-[11px] text-muted-foreground">Duur: {Math.round(last.duration_seconds/60)} min</p>}</div>}</div></section>}
    <section className="mx-auto max-w-3xl px-5 pb-5 sm:px-6"><div className="mb-3 flex items-baseline justify-between"><h2 className="text-xl text-navy">Mijn meldingen</h2><span className="text-[10px] uppercase tracking-wider text-muted-foreground">privé</span></div>{!user&&!loading&&<Link to="/auth" className="block rounded-xl border border-border bg-white p-4 text-sm text-navy shadow-sm">Log in om je meldingen te bewaren <ChevronRight size={16} className="inline"/></Link>}{user&&logs.length===0&&<div className="rounded-xl border border-dashed border-border bg-white/60 p-6 text-center text-sm text-muted-foreground">Nog geen bewonersmeldingen.</div>}{logs.length>0&&<ul className="space-y-2">{logs.map(l=>{const d=new Date(l.observed_at??l.timestamp);return <li key={l.id} className="rounded-xl border border-border bg-white px-4 py-3"><div className="flex items-center justify-between"><div><div className="text-base text-navy font-serif">{l.flight_number??"Vlucht onbekend"}</div><div className="text-[10px] text-muted-foreground">{d.toLocaleDateString("nl-NL")} · {d.toLocaleTimeString("nl-NL",{hour:"2-digit",minute:"2-digit"})}</div></div><div className="text-right"><div className="font-mono text-sm text-navy">{l.db_level??"—"} dB</div><div className="text-[10px] text-muted-foreground">eigen schatting</div></div></div>{featureFlags.noiseEvidenceV2&&l.observation_text&&<p className="mt-2 border-t border-border/60 pt-2 text-xs leading-5 text-muted-foreground">{l.observation_text}</p>}</li>})}</ul>}</section><section className="mx-auto max-w-3xl px-5 pb-12 sm:px-6"><Link to="/claim" className="flex items-center justify-between rounded-2xl bg-navy px-5 py-4 text-white shadow-sm"><div><div className="text-lg font-serif">Dossier voorbereiden</div><div className="mt-0.5 text-xs text-white/60">Orden je informatie en vervolgstappen</div></div><ChevronRight size={19}/></Link></section>
  </AppShell>;
}
