/* eslint-disable prettier/prettier */
import { Link } from "@tanstack/react-router";
import { Menu, X, Sparkles, MapPin, Store, Plane } from "lucide-react";
import { useState, type ReactNode } from "react";
import { BottomNav } from "./BottomNav";
import { useAuth } from "@/hooks/use-auth";
import { supabase } from "@/integrations/supabase/client";
import { featureFlags } from "@/config/featureFlags";

const baseNav = [["/", "Start"] as const, ["/check", "Adres"] as const];
export function AppShell({ children }: { children: ReactNode }) {
  const [open, setOpen] = useState(false);
  const { user } = useAuth();
  const close = () => setOpen(false);
  const nav = featureFlags.flightNoiseHubV1 ? [...baseNav, ["/flight-noise", "Vliegtuig & geluid"] as const] : [...baseNav, ["/log", "Melding"] as const, ["/map", "Geluidskaart"] as const, ["/claim", "Dossier"] as const];
  return <div className="app-page min-h-[100dvh] w-full bg-cream text-navy antialiased"><div className="relative mx-auto min-h-[100dvh] w-full max-w-[1600px] overflow-hidden"><header className="sticky top-0 z-50 border-b border-white/10 bg-navy/95 text-white shadow-[0_8px_30px_rgba(8,22,45,.14)] backdrop-blur-xl"><div className="mx-auto flex min-h-16 w-full max-w-7xl items-center justify-between gap-3 px-4 sm:px-6 lg:min-h-[72px] lg:px-8"><Link to="/" className="group flex min-w-0 items-center gap-2.5 rounded-2xl py-1.5 focus:outline-none focus:ring-2 focus:ring-white/30" aria-label="Uithoorn Online home"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl border border-white/15 bg-white/10 font-serif text-[18px] shadow-inner transition group-hover:bg-white/15">U</span><span className="truncate font-serif text-[19px] tracking-[-0.02em] sm:text-[21px]">uithoorn<span className="text-red">.</span>online</span></Link><nav className="hidden items-center gap-1 lg:flex" aria-label="Hoofdnavigatie">{nav.map(([to,label])=><Link key={to} to={to} activeProps={{className:"rounded-xl bg-white/12 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm"}} inactiveProps={{className:"rounded-xl px-3.5 py-2.5 text-sm font-medium text-white/70 transition hover:bg-white/8 hover:text-white"}}>{label}</Link>)}</nav><div className="flex shrink-0 items-center gap-2">{featureFlags.localServicesV1&&<Link to="/services" className="hidden items-center gap-2 rounded-xl border border-white/12 bg-white/8 px-3 py-2.5 text-xs font-semibold text-white transition hover:bg-white/14 sm:flex"><Store size={15}/> Diensten</Link>}<Link to="/check" className="hidden items-center gap-2 rounded-xl bg-red px-3.5 py-2.5 text-xs font-bold text-red-foreground shadow-lg shadow-black/10 transition hover:-translate-y-0.5 sm:flex"><MapPin size={15}/> Adres controleren</Link><button aria-label={open?"Menu sluiten":"Menu openen"} aria-expanded={open} onClick={()=>setOpen(v=>!v)} className="grid h-10 w-10 place-items-center rounded-xl border border-white/12 bg-white/5 text-white transition hover:bg-white/12 focus:outline-none focus:ring-2 focus:ring-white/30 lg:hidden">{open?<X size={20}/>:<Menu size={20}/>}</button></div></div>{open&&<div className="border-t border-white/10 bg-navy/98 px-4 pb-5 pt-3 backdrop-blur-xl"><div className="mx-auto max-w-2xl"><div className="mb-3 flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-3 py-2.5 text-[11px] text-white/65"><Sparkles size={14}/><span>Onafhankelijke inwonersservice · geen overheidsinstantie</span></div><div className="grid grid-cols-2 gap-2"><Link to="/" onClick={close} className="rounded-xl bg-white/7 px-3 py-3 text-sm font-semibold">Start</Link><Link to="/check" onClick={close} className="rounded-xl bg-white/7 px-3 py-3 text-sm">Adres controleren</Link>{featureFlags.flightNoiseHubV1?<Link to="/flight-noise" onClick={close} className="col-span-2 flex items-center gap-2 rounded-xl bg-white/7 px-3 py-3 text-sm font-semibold"><Plane size={16}/> Vliegtuig &amp; geluid</Link>:<><Link to="/log" onClick={close} className="rounded-xl bg-white/7 px-3 py-3 text-sm">Geluid melden</Link><Link to="/map" onClick={close} className="rounded-xl bg-white/7 px-3 py-3 text-sm">Geluidskaart</Link><Link to="/claim" onClick={close} className="rounded-xl bg-white/7 px-3 py-3 text-sm">Dossier voorbereiden</Link></>}{featureFlags.localServicesV1&&<><Link to="/services" onClick={close} className="rounded-xl bg-white/7 px-3 py-3 text-sm">Lokale diensten</Link><Link to="/services/provider" onClick={close} className="rounded-xl bg-white/7 px-3 py-3 text-sm">Voor bedrijven</Link></>}</div><div className="mt-3 flex flex-wrap gap-x-5 gap-y-2 border-t border-white/10 pt-3 text-[11px] text-white/55"><Link to="/privacy" onClick={close}>Privacy &amp; AVG</Link><Link to="/voorwaarden" onClick={close}>Voorwaarden</Link>{user?<button onClick={async()=>{await supabase.auth.signOut();close();}}>Uitloggen</button>:<Link to="/auth" onClick={close}>Inloggen / Registreren</Link>}</div></div></div>}</header><div className="border-b border-border/70 bg-white/80 px-4 py-2.5 text-[10px] leading-snug text-navy/65 backdrop-blur sm:px-6 lg:px-8"><div className="mx-auto flex max-w-7xl items-center gap-2"><span className="h-1.5 w-1.5 shrink-0 rounded-full bg-red"/><span><strong>Informatieservice:</strong> Uithoorn Online is geen overheidsinstantie, advocaat of beslissende autoriteit.</span></div></div><main>{children}</main><BottomNav/></div></div>;
}
